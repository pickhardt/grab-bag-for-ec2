# Credentials are in a separate file so you can easily .gitignore
# the entire file, if you like, to keep the credential out of
# any version control systems.

# *REPLACE* Replace with your security credentials:
AWS_ACCESS_KEY_ID = "REPLACE_ME"
AWS_SECRET_ACCESS_KEY = "REPLACE_ME"
